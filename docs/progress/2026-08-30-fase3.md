# Fase 3 em um dia — 2026-08-30

Doze blocos entregues em `main` (cada um com testes, docs e CI verde). Estado: **critérios do
gate F3 atendidos**; release `0.3-storage` aguarda a ordem 0.1 → 0.2 → 0.3.

| Bloco | Entrega | Evidência |
|---|---|---|
| 1 | PCI + concessões de dispositivo (syscalls 17–23) + `blockdev` VirtIO em ring 3 | testes `pci`, `user_block`; cenário `storage` |
| 2 | NexoFS v0 (commits atômicos por setor) + `fs` + `tools/nexo-disk` | corte de energia em cada escrita (host); `user_fs` |
| 3 | `device_open` (concessão por função), `devmgr`, `nexo-virtio`, `rngdev` | `user_devmgr`; cenário `powercut` |
| 4 | FAT12/16/32 só leitura + GPT (`nexo-fat`) + `espfs` lendo a ESP real | teste com FAT32 do mtools; `utest: esp ok` |
| 5 | VFS com namespace por processo + ramfs `/tmp` + cache de blocos | `user_vfs` (2 namespaces isolados) |
| 6 | VirtIO-console + shell de diagnóstico interativo | cenário `shell` (13 comandos por socket UNIX) |
| 7 | VirtIO-input com teclas reais injetadas por QMP | cenário `input` (códigos evdev conferidos) |
| 8 | IDL própria + gerador (`tools/idlgen`) + cabeçalho NXIP + `nexo.rng` | `cargo test -p nexo-proto`; `make idl-check` no CI |
| 9 | Fuzzing contínuo: `make fuzz` (sementes aleatórias logadas) + workflow semanal | execução manual do workflow: PASS |
| 10–12 | Todos os protocolos de serviço migrados para a IDL (block, console, input, fs, esp) | suíte de 9 cenários PASS após cada bloco |

Também hoje: stress de 2 h PASS (17 M trocas de contexto, contador com lock exato —
`2026-08-30-stress-2h.md`); as **24 h do gate F1 estão em execução**
(`make stress DURATION=86400 SMP=4`, log `build/logs/stress-long.log` — conferir
`[STRESS] PASS duracao=86400s` ao final e escrever o relatório).

Pendências da Fase 3: tradução IOMMU (abstração e caminho inseguro explícito existem),
fila assíncrona de blocos, binding por propriedades, release `0.3-storage`.
