# third_party/

Dependências vendorizadas com seus textos de licença (ADR-0012). Vazio nesta release: o kernel não tem dependências externas e o loader obtém `uefi`/`log` do crates.io com versões fixadas em `boot/loader/Cargo.lock`. A vendorização torna-se obrigatória a partir de `0.2`.
