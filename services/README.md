# services/

Reservado para os serviços de usuário (Plano Mestre §3.6): `init`, `service-manager`, `device-manager`, `vfs`, `storage-manager`, `network-stack`, `security-broker`, `package-manager`, `update-manager`, `session-manager`, `input-service`, `compositor`, `audio-service`, `settings-service`, `log-service`, `power-manager`. Começa na Fase 2 (`0.2-userspace`) com `init` e `service-manager`.
