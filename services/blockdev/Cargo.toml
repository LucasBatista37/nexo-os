[package]
name = "nexo-blockdev"
description = "Driver VirtIO-block (PCI moderno, MSI-X) em processo de usuario; serve leitura/escrita de setores por canal"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[dependencies]
nexo-sys.workspace = true
nexo-rt = { workspace = true, features = ["panic-handler"] }

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
