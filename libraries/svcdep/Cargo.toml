[package]
name = "nexo-svcdep"
description = "Ordem de partida de servicos a partir de dependencias declaradas (no_std, sem alocacao)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
