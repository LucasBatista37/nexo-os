[package]
name = "nexo-netstack"
description = "Camadas basicas de rede do Nexo OS: Ethernet, ARP, IPv4 (checksum) e ICMP echo — sem alocacao, testavel no host"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true
