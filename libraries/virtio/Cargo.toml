[package]
name = "nexo-virtio"
description = "Transporte VirtIO 1.x sobre PCI (capabilities modernas, fila dividida, MSI-X) para drivers em modo usuario — sem alocacao, parser testavel no host"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true
